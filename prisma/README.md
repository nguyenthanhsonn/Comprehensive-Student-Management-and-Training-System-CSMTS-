# Prisma Layer

Thu muc nay chua schema, migration va seed cho PostgreSQL.

- `schema`: tach schema theo domain neu can.
- `migrations`: lich su migration.
- `seeds`: du lieu mau hoac du lieu khoi tao.
